#![allow(unused)]

use rand::Rng;
use std::cmp::Ordering;
use std::fs::File;
use std::io;
use std::io::{BufRead, BufReader, ErrorKind, Write};


fn main() {
    // _is_true: adding _ on the beginning the compile ignore errors
    let is_true = true;
    let my_grade = 'A';
}